#!/usr/bin/env bash
set -euo pipefail
root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec cargo run --locked --release \
  --manifest-path "$root/experiments/external_funded_runner/Cargo.toml" \
  --bin external-funded-runner -- "$@"
